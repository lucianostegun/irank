fields:
       
  eventId:
    required:
      msg:       Este campo é obrigatório e não foi preenchido
    sfCallbackValidator:
      callback:      [EventMemberPeer, validateResult]
      invalid_error: erro