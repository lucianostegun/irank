fields:
       
  firstName:
    required:
      msg:       Este campo é obrigatório e não foi preenchido
    sfStringValidator:
      max:       25
      max_error: Informe um valor de até 25 caracteres
       
  emailAddress:
    required:
      msg:       Este campo é obrigatório e não foi preenchido
    sfStringValidator:
      max:       150
      max_error: Informe um valor de até 150 caracteres
    sfEmailValidator:
      strict:       true
      email_error:  O e-mail informado não é um e-mail válido