fields:
       
  placeName:
    required:
      msg:       form.error.requiredField
    sfStringValidator:
      max:       20
      max_error: Informe um valor de até 20 caracteres
    sfCallbackValidator:
      callback:      [RankingPlacePeer, uniquePlaceName]
      invalid_error: Já existe um local cadastrado com esse nome
       
  mapsLink:
    required:
      msg:       form.error.requiredField
    sfUrlValidator:
      url_error: A URL informada não é válida